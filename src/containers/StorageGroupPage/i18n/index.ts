import {registerKeysets} from '../../../utils/i18n';

import en from './en.json';

const COMPONENT = 'ydb-storage-group-page';

export const storageGroupPageKeyset = registerKeysets(COMPONENT, {en});
