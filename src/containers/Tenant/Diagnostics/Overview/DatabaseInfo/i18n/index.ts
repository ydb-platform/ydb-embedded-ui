import {registerKeysets} from '../../../../../../utils/i18n';

import en from './en.json';

const COMPONENT = 'ydb-diagnostics-database-info';

export const dbInfoKeyset = registerKeysets(COMPONENT, {en});
