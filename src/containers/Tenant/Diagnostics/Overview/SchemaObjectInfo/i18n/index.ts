import {registerKeysets} from '../../../../../../utils/i18n';

import en from './en.json';

const COMPONENT = 'ydb-schema-object-info';

export const schemaObjectInfoKeyset = registerKeysets(COMPONENT, {en});
