import {registerKeysets} from '../../../../../../utils/i18n';

import en from './en.json';

// Tenant storage overview: summary cards, segment legends, quota warnings, and top usage table.
const COMPONENT = 'ydb-diagnostics-tenant-storage';

export default registerKeysets(COMPONENT, {en});
